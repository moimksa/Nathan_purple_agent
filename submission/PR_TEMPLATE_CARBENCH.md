# CAR-bench Submission PR Template

## Submission Summary

- Team/Handle:
- Agent name:
- AgentBeats ID (`AGENTBEATS_ID`):
- Model (`AGENT_LLM`):
- Docker image (GHCR):
- Commit SHA (agent repo):

## Scenario Settings

- `num_trials = 3`
- `task_split = "test"`
- `tasks_base_num_tasks = -1`
- `tasks_hallucination_num_tasks = -1`
- `tasks_disambiguation_num_tasks = -1`
- `max_steps = 50`

## Verification Checklist

- [ ] I confirm this PR is from a non-default branch.
- [ ] I confirm required secrets are configured in the fork.
- [ ] I confirm `scenario.toml` placeholders are fully replaced.
- [ ] I understand this PR may trigger cost-incurring evaluation workflows.

## Notes for Reviewers

- Optional config notes:
- Known limitations:
